// Evidências/documentos por fase e grande marco do projeto.
// Metadados no D1; bytes em R2 quando disponível ou D1 chunked como fallback sem cartão.
const meWrite=['admin','pmo','techlead','gestor'].includes(user.role);
const meScope=id=>!scope||String(id)===String(scope);
const meNew=p=>p+'-'+Date.now().toString(36).toUpperCase()+'-'+crypto.randomUUID().slice(0,6).toUpperCase();
const meSafe=(v,n=5000)=>String(v??'').slice(0,n);
const meChunkSize=1500000;
const meProject=async id=>DB.prepare('SELECT id,name,company_id FROM projects WHERE id=?').bind(id).first();
const meContext=async(companyId,projectId)=>{
  if(!companyId||!projectId)return {error:'Empresa e projeto são obrigatórios',status:400};
  if(!meScope(companyId))return {error:'Fora do escopo',status:403};
  const c=await DB.prepare('SELECT id,name FROM companies WHERE id=?').bind(companyId).first();if(!c)return {error:'Empresa não encontrada',status:404};
  const p=await meProject(projectId);if(!p)return {error:'Projeto não encontrado',status:404};
  if(String(p.company_id)!==String(companyId))return {error:'Projeto não pertence à empresa informada',status:400};
  return {company:c,project:p};
};
let meSchemaReady=false;
const meEnsureStorageSchema=async()=>{
  if(meSchemaReady)return;
  const stmts=[
    DB.prepare("CREATE TABLE IF NOT EXISTS project_milestone_details (company_id TEXT NOT NULL,project_id INTEGER NOT NULL,phase_key TEXT NOT NULL,milestone_key TEXT NOT NULL,phase_title TEXT DEFAULT '',milestone_title TEXT DEFAULT '',description TEXT DEFAULT '',subdescription TEXT DEFAULT '',phase_rank INTEGER NOT NULL DEFAULT 0,milestone_rank INTEGER NOT NULL DEFAULT 0,updated_by TEXT,updated_at TEXT NOT NULL DEFAULT (datetime('now')),PRIMARY KEY(company_id,project_id,phase_key,milestone_key))"),
    DB.prepare("CREATE TABLE IF NOT EXISTS project_milestone_assets (id TEXT PRIMARY KEY,company_id TEXT NOT NULL,project_id INTEGER NOT NULL,phase_key TEXT NOT NULL,milestone_key TEXT NOT NULL,phase_title TEXT DEFAULT '',milestone_title TEXT DEFAULT '',asset_type TEXT NOT NULL DEFAULT 'LINK',title TEXT DEFAULT '',description TEXT DEFAULT '',link_url TEXT,object_key TEXT,file_name TEXT,mime_type TEXT,size_bytes INTEGER NOT NULL DEFAULT 0,client_visible INTEGER NOT NULL DEFAULT 1,created_by TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),archived_at TEXT)"),
    DB.prepare("CREATE TABLE IF NOT EXISTS tenant_files (id TEXT PRIMARY KEY,company_id TEXT NOT NULL,project_id INTEGER,entity_type TEXT NOT NULL DEFAULT 'PROJECT',entity_id TEXT NOT NULL,category TEXT NOT NULL DEFAULT 'DOCUMENTO',title TEXT DEFAULT '',description TEXT DEFAULT '',object_key TEXT NOT NULL,file_name TEXT NOT NULL,mime_type TEXT DEFAULT '',size_bytes INTEGER NOT NULL DEFAULT 0,version_no INTEGER NOT NULL DEFAULT 1,client_visible INTEGER NOT NULL DEFAULT 1,status TEXT NOT NULL DEFAULT 'ACTIVE',created_by TEXT,created_at TEXT NOT NULL DEFAULT (datetime('now')),archived_at TEXT)"),
    DB.prepare("CREATE TABLE IF NOT EXISTS tenant_file_chunks (file_id TEXT NOT NULL,company_id TEXT NOT NULL,project_id INTEGER,chunk_no INTEGER NOT NULL,data_blob BLOB NOT NULL,size_bytes INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT (datetime('now')),PRIMARY KEY(file_id,chunk_no))")
  ];
  await DB.batch(stmts);meSchemaReady=true;
};
const meAsset=async id=>{await meEnsureStorageSchema();return DB.prepare('SELECT * FROM project_milestone_assets WHERE id=? AND archived_at IS NULL').bind(id).first()};
const meBytes=v=>v instanceof ArrayBuffer?new Uint8Array(v):ArrayBuffer.isView(v)?new Uint8Array(v.buffer,v.byteOffset,v.byteLength):Array.isArray(v)?Uint8Array.from(v):new Uint8Array();
const meStoreD1=async(id,company,project,buf)=>{await meEnsureStorageSchema();for(let off=0,n=0;off<buf.byteLength;off+=meChunkSize,n++){const piece=buf.slice(off,Math.min(off+meChunkSize,buf.byteLength));await DB.prepare('INSERT INTO tenant_file_chunks(file_id,company_id,project_id,chunk_no,data_blob,size_bytes) VALUES(?,?,?,?,?,?)').bind(id,company,project,n,piece,piece.byteLength).run()}return 'd1:'+id};
const meReadD1=async id=>{await meEnsureStorageSchema();const rows=(await DB.prepare('SELECT data_blob FROM tenant_file_chunks WHERE file_id=? ORDER BY chunk_no ASC').bind(id).all()).results||[];if(!rows.length)return null;const chunks=rows.map(r=>meBytes(r.data_blob)),total=chunks.reduce((s,b)=>s+b.byteLength,0),out=new Uint8Array(total);let pos=0;for(const b of chunks){out.set(b,pos);pos+=b.byteLength}return out};
const meStore=async({id,company,project,key,buffer,mime,metadata})=>{if(env.DOCS){await env.DOCS.put(key,buffer,{httpMetadata:{contentType:mime||'application/octet-stream'},customMetadata:metadata});return {key,backend:'R2'}}return {key:await meStoreD1(id,company,project,buffer),backend:'D1'}};
const meRead=async a=>{if(String(a.object_key||'').startsWith('d1:'))return {body:await meReadD1(a.id),mime:a.mime_type};if(!env.DOCS||!a.object_key)return null;const obj=await env.DOCS.get(a.object_key);if(!obj)return null;return {body:obj.body,mime:a.mime_type||obj.httpMetadata?.contentType}};
const meAiText=result=>{if(typeof result==='string')return result;if(typeof result?.response==='string')return result.response;if(typeof result?.result?.response==='string')return result.result.response;return ''};
const mePhaseTemplate=(phase,notes,existing)=>`# ${phase}\n\n## Contexto e objetivo\n${existing||'A confirmar com base no planejamento e nas reuniões da fase.'}\n\n## Registro da reunião / evidência utilizada\n${notes||'Não informado.'}\n\n## Escopo da fase\nA confirmar.\n\n## Atividades e entregáveis\n- A confirmar.\n\n## Decisões tomadas\n- A confirmar.\n\n## Pendências e responsáveis\n- A confirmar.\n\n## Riscos e impedimentos\n- A confirmar.\n\n## Dependências e premissas\n- A confirmar.\n\n## Critérios de conclusão / aceite\n- A confirmar.\n\n## Próximos passos\n- A confirmar.\n`;

if(path==='project-milestone-evidence'&&request.method==='GET'){
  const company=url.searchParams.get('company'),project=url.searchParams.get('project');const ctx=await meContext(company,project);if(ctx.error)return json({error:ctx.error},ctx.status);
  try{await meEnsureStorageSchema();const details=(await DB.prepare('SELECT * FROM project_milestone_details WHERE company_id=? AND project_id=? ORDER BY phase_rank ASC,milestone_rank ASC,updated_at DESC').bind(company,project).all()).results||[];const assets=(await DB.prepare('SELECT id,company_id,project_id,phase_key,milestone_key,phase_title,milestone_title,asset_type,title,description,link_url,object_key,file_name,mime_type,size_bytes,client_visible,created_by,created_at FROM project_milestone_assets WHERE company_id=? AND project_id=? AND archived_at IS NULL ORDER BY created_at DESC').bind(company,project).all()).results||[];return json({company:ctx.company,project:ctx.project,details,assets:assets.map(a=>({...a,storage_backend:a.asset_type==='FILE'?(String(a.object_key||'').startsWith('d1:')?'D1':'R2'):null})),storage:{r2:!!env.DOCS,d1:true,backend:env.DOCS?'R2':'D1'}})}catch(err){console.error('[milestone-evidence:get]',err);return json({error:'Não foi possível carregar a documentação desta fase agora.',retryable:true},500)}
}

if(path==='project-milestone-details'&&request.method==='POST'){
  if(!meWrite)return json({error:'Sem permissão'},403);const b=await request.json();const ctx=await meContext(b.company_id,b.project_id);if(ctx.error)return json({error:ctx.error},ctx.status);
  const phaseKey=meSafe(b.phase_key,180),milestoneKey=meSafe(b.milestone_key,180);if(!phaseKey||!milestoneKey)return json({error:'Fase e item documental são obrigatórios'},400);
  try{await meEnsureStorageSchema();await DB.prepare("INSERT INTO project_milestone_details(company_id,project_id,phase_key,milestone_key,phase_title,milestone_title,description,subdescription,phase_rank,milestone_rank,updated_by,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,datetime('now')) ON CONFLICT(company_id,project_id,phase_key,milestone_key) DO UPDATE SET phase_title=excluded.phase_title,milestone_title=excluded.milestone_title,description=excluded.description,subdescription=excluded.subdescription,phase_rank=excluded.phase_rank,milestone_rank=excluded.milestone_rank,updated_by=excluded.updated_by,updated_at=datetime('now')").bind(b.company_id,b.project_id,phaseKey,milestoneKey,meSafe(b.phase_title,500),meSafe(b.milestone_title,500),meSafe(b.description,50000),meSafe(b.subdescription,50000),Number(b.phase_rank||0),Number(b.milestone_rank||0),user.name).run();await logEvent(env,user,milestoneKey==='__phase__'?'fase:documentar':'marco:detalhar',`${b.company_id}/${b.project_id}/${phaseKey}/${milestoneKey}`,meSafe(b.phase_title||b.milestone_title,500));return json({ok:true})}catch(err){console.error('[milestone-details:save]',err);return json({error:'Não foi possível salvar a documentação agora. Tente novamente.',retryable:true},500)}
}

if(path==='project-phase-ai'&&request.method==='POST'){
  if(!meWrite)return json({error:'Sem permissão'},403);const b=await request.json();const ctx=await meContext(b.company_id,b.project_id);if(ctx.error)return json({error:ctx.error},ctx.status);const phase=meSafe(b.phase_title||'Fase do projeto',500),notes=meSafe(b.meeting_notes,50000),existing=meSafe(b.existing_description,30000);if(!notes.trim())return json({error:'Cole o resumo ou a transcrição da reunião para o assistente trabalhar.'},400);
  const instructions=`Você é o Assistente PMO do Instituto Államo. Transforme notas de reunião em documentação profissional da fase de um projeto. Não invente fatos, datas, responsáveis, percentuais ou decisões. Quando faltar informação escreva "A confirmar". Entregue Markdown em português do Brasil, objetivo e executivo, com as seções: Contexto e objetivo; Escopo da fase; Atividades e entregáveis; Decisões tomadas; Pendências e responsáveis; Riscos e impedimentos; Dependências e premissas; Critérios de conclusão/aceite; Próximos passos; Evidências/documentos citados. Preserve fatos da documentação existente quando não forem contraditos pelas notas.`;
  const input=`Empresa: ${ctx.company.name}\nProjeto: ${ctx.project.name}\nFase: ${phase}\n\nDocumentação existente:\n${existing||'Não informada.'}\n\nResumo/transcrição da reunião:\n${notes}`;
  let documentation='',provider='TEMPLATE',model='';
  if(env.AI){try{model=env.CLOUDFLARE_REPORT_MODEL||'@cf/meta/llama-3.1-8b-instruct-fp8-fast';const out=await env.AI.run(model,{messages:[{role:'system',content:instructions},{role:'user',content:input}],max_tokens:4200,temperature:0.15});documentation=meAiText(out).trim();if(documentation)provider='CLOUDFLARE_WORKERS_AI'}catch(err){console.error('[project-phase-ai]',err)}}
  if(!documentation)documentation=mePhaseTemplate(phase,notes,existing);
  await logEvent(env,user,'fase:assistente-ia',`${b.company_id}/${b.project_id}/${meSafe(b.phase_key,180)}`,`${phase} · ${provider}`);
  return json({ok:true,provider,model:model||null,documentation,approval_required:true,warning:provider==='TEMPLATE'?'IA indisponível no momento; foi criado um modelo estruturado para revisão.':'Revise o conteúdo antes de salvar. A IA não grava automaticamente.'});
}

if(path==='project-milestone-assets'&&request.method==='POST'){
  if(!meWrite)return json({error:'Sem permissão'},403);const b=await request.json();const ctx=await meContext(b.company_id,b.project_id);if(ctx.error)return json({error:ctx.error},ctx.status);const type=String(b.asset_type||'LINK').toUpperCase();if(type!=='LINK')return json({error:'Use a rota de upload para arquivos'},400);if(!/^https?:\/\//i.test(String(b.link_url||'')))return json({error:'Informe um link http(s) válido'},400);
  try{await meEnsureStorageSchema();const id=meNew('MEA');await DB.prepare('INSERT INTO project_milestone_assets(id,company_id,project_id,phase_key,milestone_key,phase_title,milestone_title,asset_type,title,description,link_url,client_visible,created_by) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(id,b.company_id,b.project_id,meSafe(b.phase_key,180),meSafe(b.milestone_key,180),meSafe(b.phase_title,500),meSafe(b.milestone_title,500),'LINK',meSafe(b.title,500),meSafe(b.description,5000),meSafe(b.link_url,5000),b.client_visible===false?0:1,user.name).run();await logEvent(env,user,b.milestone_key==='__phase__'?'fase:link':'marco:link',id,meSafe(b.title,500));return json({ok:true,id},201)}catch(err){console.error('[milestone-link]',err);return json({error:'Não foi possível adicionar o link agora.',retryable:true},500)}
}

if(path==='project-milestone-assets/upload'&&request.method==='POST'){
  if(!meWrite)return json({error:'Sem permissão'},403);
  try{
    await meEnsureStorageSchema();const form=await request.formData();const company=form.get('company_id'),project=form.get('project_id');const ctx=await meContext(company,project);if(ctx.error)return json({error:ctx.error},ctx.status);const file=form.get('file');if(!file||typeof file.arrayBuffer!=='function')return json({error:'Selecione um arquivo'},400);const max=20*1024*1024;if(Number(file.size||0)>max)return json({error:'Arquivo excede 20 MB'},413);
    const id=meNew('MEA'),safeName=String(file.name||'arquivo').replace(/[^a-zA-Z0-9._-]+/g,'_').slice(-160),milestoneKey=meSafe(form.get('milestone_key'),180),phaseKey=meSafe(form.get('phase_key'),180),isPhase=milestoneKey==='__phase__',entityType=isPhase?'PHASE':'MILESTONE',desiredKey=`${company}/${project}/${entityType}/${id}/${safeName}`;const title=meSafe(form.get('title')||file.name,500),description=meSafe(form.get('description'),5000),visible=String(form.get('client_visible')||'1')==='0'?0:1,buf=await file.arrayBuffer();
    const stored=await meStore({id,company,project,key:desiredKey,buffer:buf,mime:file.type,metadata:{company_id:String(company),project_id:String(project),asset_id:id,entity_type:entityType,entity_id:isPhase?phaseKey:milestoneKey}});
    await DB.prepare('INSERT INTO project_milestone_assets(id,company_id,project_id,phase_key,milestone_key,phase_title,milestone_title,asset_type,title,description,object_key,file_name,mime_type,size_bytes,client_visible,created_by) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(id,company,project,phaseKey,milestoneKey,meSafe(form.get('phase_title'),500),meSafe(form.get('milestone_title'),500),'FILE',title,description,stored.key,meSafe(file.name,500),meSafe(file.type,200),Number(file.size||0),visible,user.name).run();
    await DB.prepare('INSERT INTO tenant_files(id,company_id,project_id,entity_type,entity_id,category,title,description,object_key,file_name,mime_type,size_bytes,version_no,client_visible,status,created_by) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').bind(id,company,project,entityType,isPhase?phaseKey:milestoneKey,isPhase?'FASE':'MARCO',title,description,stored.key,meSafe(file.name,500),meSafe(file.type,200),Number(file.size||0),1,visible,'ACTIVE',user.name).run();await logEvent(env,user,isPhase?'fase:arquivo':'marco:arquivo',id,`${meSafe(file.name,500)} [${stored.backend}]`);return json({ok:true,id,file_name:file.name,storage_backend:stored.backend},201)
  }catch(err){console.error('[milestone-upload]',err);return json({error:'Não foi possível anexar o arquivo agora. A página continua segura; tente novamente.',retryable:true},500)}
}

if(path.match(/^project-milestone-assets\/[^/]+\/content$/)&&request.method==='GET'){
  const id=decodeURIComponent(path.split('/')[1]),a=await meAsset(id);if(!a)return json({error:'Arquivo não encontrado'},404);if(!meScope(a.company_id))return json({error:'Fora do escopo'},403);const ctx=await meContext(a.company_id,a.project_id);if(ctx.error)return json({error:ctx.error},ctx.status);if(a.asset_type!=='FILE'||!a.object_key)return json({error:'Este item não é um arquivo'},400);const obj=await meRead(a);if(!obj||!obj.body)return json({error:'Conteúdo do arquivo não encontrado'},404);const h=new Headers();h.set('content-type',obj.mime||'application/octet-stream');h.set('content-disposition',`inline; filename="${String(a.file_name||'arquivo').replace(/["\r\n]/g,'')}"`);h.set('cache-control','private, max-age=60');return new Response(obj.body,{headers:h});
}

if(path.match(/^public-milestone-assets\/[^/]+\/content$/)&&request.method==='GET'){
  const id=decodeURIComponent(path.split('/')[1]),company=url.searchParams.get('company');if(!company)return json({error:'Informe a empresa'},400);await meEnsureStorageSchema();const a=await DB.prepare('SELECT * FROM project_milestone_assets WHERE id=? AND company_id=? AND client_visible=1 AND archived_at IS NULL').bind(id,company).first();if(!a)return json({error:'Arquivo não disponível para esta empresa'},404);const p=await meProject(a.project_id);if(!p||String(p.company_id)!==String(company))return json({error:'Contexto inválido'},403);if(a.asset_type!=='FILE'||!a.object_key)return json({error:'Arquivo indisponível'},404);const obj=await meRead(a);if(!obj||!obj.body)return json({error:'Arquivo indisponível'},404);const h=new Headers();h.set('content-type',obj.mime||'application/octet-stream');h.set('content-disposition',`inline; filename="${String(a.file_name||'arquivo').replace(/["\r\n]/g,'')}"`);return new Response(obj.body,{headers:h});
}

if(path.match(/^project-milestone-assets\/[^/]+$/)&&request.method==='DELETE'){
  if(!meWrite)return json({error:'Sem permissão'},403);const id=decodeURIComponent(path.split('/')[1]),a=await meAsset(id);if(!a)return json({error:'Item não encontrado'},404);if(!meScope(a.company_id))return json({error:'Fora do escopo'},403);const ctx=await meContext(a.company_id,a.project_id);if(ctx.error)return json({error:ctx.error},ctx.status);await DB.prepare("UPDATE project_milestone_assets SET archived_at=datetime('now') WHERE id=?").bind(id).run();if(a.asset_type==='FILE')try{await DB.prepare("UPDATE tenant_files SET status='ARCHIVED',archived_at=COALESCE(archived_at,datetime('now')) WHERE id=?").bind(id).run()}catch(_){}await logEvent(env,user,a.milestone_key==='__phase__'?'fase:evidencia-arquivar':'marco:evidencia-arquivar',id,a.title||a.file_name||'');return json({ok:true,archived:true});
}
