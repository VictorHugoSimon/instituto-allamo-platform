// Governança de projeto no Stage: criação idempotente e não destrutiva.
if (isAllamoStage) {
  await stageSafe("CREATE TABLE IF NOT EXISTS governance_events (id TEXT PRIMARY KEY, company_id TEXT NOT NULL, project_id INTEGER NOT NULL, report_id TEXT, roadmap_item_id TEXT, event_type TEXT NOT NULL DEFAULT 'REUNIAO', title TEXT NOT NULL, description TEXT DEFAULT '', area TEXT DEFAULT '', sector TEXT DEFAULT '', start_at TEXT, end_at TEXT, location TEXT DEFAULT '', meeting_url TEXT DEFAULT '', recurrence_rule TEXT DEFAULT '', status TEXT NOT NULL DEFAULT 'PLANEJADA', minutes_summary TEXT DEFAULT '', decisions_summary TEXT DEFAULT '', client_visible INTEGER NOT NULL DEFAULT 1, created_by TEXT, updated_by TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')), archived_at TEXT)");
  await stageSafe("CREATE TABLE IF NOT EXISTS governance_event_agenda_items (id TEXT PRIMARY KEY, event_id TEXT NOT NULL, company_id TEXT NOT NULL, project_id INTEGER NOT NULL, title TEXT NOT NULL, description TEXT DEFAULT '', area TEXT DEFAULT '', owner_name TEXT DEFAULT '', status TEXT NOT NULL DEFAULT 'ABERTA', rank REAL NOT NULL DEFAULT 0, client_visible INTEGER NOT NULL DEFAULT 1, created_by TEXT, updated_by TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')), archived_at TEXT)");
  await stageSafe("CREATE TABLE IF NOT EXISTS governance_event_stakeholders (id TEXT PRIMARY KEY, event_id TEXT NOT NULL, company_id TEXT NOT NULL, project_id INTEGER NOT NULL, stakeholder_type TEXT NOT NULL DEFAULT 'INTERNO', name TEXT NOT NULL, email TEXT DEFAULT '', role_name TEXT DEFAULT '', area TEXT DEFAULT '', attendance_status TEXT NOT NULL DEFAULT 'CONVIDADO', client_visible INTEGER NOT NULL DEFAULT 1, created_by TEXT, updated_by TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')), archived_at TEXT)");
  await stageSafe("CREATE TABLE IF NOT EXISTS governance_event_work_links (id TEXT PRIMARY KEY, event_id TEXT NOT NULL, company_id TEXT NOT NULL, project_id INTEGER NOT NULL, work_item_id TEXT NOT NULL, relation_type TEXT NOT NULL DEFAULT 'DEMANDA', client_visible INTEGER NOT NULL DEFAULT 1, created_by TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), archived_at TEXT, UNIQUE(event_id,work_item_id,relation_type))");
  await stageSafe("CREATE TABLE IF NOT EXISTS governance_event_decisions (id TEXT PRIMARY KEY, event_id TEXT NOT NULL, company_id TEXT NOT NULL, project_id INTEGER NOT NULL, title TEXT NOT NULL, decision_text TEXT DEFAULT '', owner_name TEXT DEFAULT '', due_date TEXT, status TEXT NOT NULL DEFAULT 'ABERTA', client_visible INTEGER NOT NULL DEFAULT 1, created_by TEXT, updated_by TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')), archived_at TEXT)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_events_context ON governance_events(company_id,project_id,archived_at,start_at)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_events_report ON governance_events(report_id,archived_at,start_at)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_events_status ON governance_events(company_id,project_id,status,start_at)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_agenda_event ON governance_event_agenda_items(event_id,archived_at,rank)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_stakeholders_event ON governance_event_stakeholders(event_id,archived_at,name)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_work_event ON governance_event_work_links(event_id,archived_at,work_item_id)");
  await stageSafe("CREATE INDEX IF NOT EXISTS idx_governance_decisions_event ON governance_event_decisions(event_id,archived_at,due_date)");
  await stageSafe("INSERT OR REPLACE INTO stage_runtime_flags(key,applied_at,detail) VALUES ('governance-roadmap-schema',datetime('now'),'Agendas, reuniões, stakeholders, decisões e demandas em modo multitenant persistente')");
}
