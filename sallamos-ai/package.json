{
  "name": "sallamos-ai",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler check && wrangler deploy --dry-run",
    "types": "wrangler types",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply sallamos-ai-meta --remote",
    "evals": "node evals/runner.mjs",
    "provision": "node scripts/provision.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "wrangler": "^4.68.0",
    "typescript": "^5.6.0"
  }
}
