import fs from 'node:fs';
const s=fs.readFileSync('scripts/enforce-live-first-paint.mjs','utf8');
const must=(n,l)=>{if(!s.includes(n))throw new Error(`Ausente: ${l} (${n})`)};
must('window.__allamoRevealWhenReady=function()','Função de sincronização');
must('window.__allamoBootGuardStarted','Proteção contra dupla inicialização');
must('window.__allamoBootNonBlocking=true','Modo não bloqueante');
must('setTimeout(function(){window.__allamoRevealWhenReady()},0)','Auto-start');
must('if(!hasSession()&&!isPublic()){done();return}','Fallback anônimo');
must('public-client-projects','Contexto público por empresa');
must('Sincronizando dados','Status de sincronização');
must('allamo-boot-retry','Retry de conectividade');
if(s.includes('body{visibility:hidden!important}'))throw new Error('First paint ainda esconde o body inteiro.');
console.log('OK: sincronização inicial é tenant-safe e não bloqueia a interface inteira.');
