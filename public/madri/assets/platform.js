(()=>{
'use strict';let report=null,project=null;
const officialRoutes=[['/madri-plano-de-acao/','Plano de Ação'],['/madri-status-report/','Status Report'],['/madri-pop/','POP'],['/madri-mapa-implantacao/','Mapa Mestre']];
const supportRoutes=[['/madri/','Portal'],['/madri-requisitos/','Requisitos'],['/madri-plano-testes/','Testes'],['/madri-riscos/','Riscos'],['/madri-integracoes/','Integrações'],['/madri-documentos/','Documentos']];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(path,opt={}){const headers={'content-type':'application/json',...(opt.headers||{})};const r=await fetch(path,{...opt,headers,cache:'no-store'});const text=await r.text();let data;try{data=JSON.parse(text)}catch{data=text}if(!r.ok){const e=new Error((data&&data.error)||`HTTP ${r.status}`);e.status=r.status;e.data=data;throw e}return data}
async function resolveProject(){if(project)return project;project=await api('/api/madri-platform/context');return project}
async function loadReport(force=false){if(report&&!force)return report;try{report=await api('/api/madri-platform/status-report')}catch{report=await api('/api/public-madri-pmo-report')}return report}
function showMessage(text,type='ok'){const box=document.getElementById('messageArea');if(!box)return;box.innerHTML=text?`<div class="message ${type==='error'?'error':'ok'}">${esc(text)}</div>`:'';if(text)setTimeout(()=>{if(box.textContent===text)box.innerHTML=''},6000)}
function badge(status){const s=String(status||'A confirmar'),n=s.toLowerCase();let c='';if(/conclu|aprov|atendid|homolog|produção|tomada|fechado|verde/.test(n))c='good';else if(/atras|bloque|reprov|vermelho|sev1|crít|no-go/.test(n))c='crit';else if(/andamento|atenção|pendente|revis|amarelo/.test(n))c='warn';else if(/sit|uat|e2e|pronto|desenvolvimento/.test(n))c='blue';return `<span class="pill ${c}">${esc(s)}</span>`}
const links=(items,active)=>items.map(([u,l])=>`<a class="${active===l?'on':''}" href="${u}">${l}</a>`).join('');
function nav(active){return `<div class="brand"><div class="eyebrow">Instituto Államo · PMO</div><h1>MADRI</h1><small>Governança com URLs permanentes</small></div><div class="navlabel">Links oficiais permanentes</div><div class="nav">${links(officialRoutes,active)}</div><div class="navlabel">Módulos de apoio</div><div class="nav">${links(supportRoutes,active)}</div><div class="side-note"><b>Fonte única e rastreável.</b><br>Reunião / Documento / Cliente / Fornecedor / PMO → Evidência → Ação / Decisão / Risco → Plano de Ação → Banco → Execução → Evidência → POP / Mapa Mestre → Status Report.<br><br><b>Regra:</b> URLs permanentes; versões no Git; dados operacionais no D1.</div>`}
async function init(active,title,subtitle){const side=document.getElementById('sidebar');if(side)side.innerHTML=nav(active);const t=document.getElementById('pageTitle');if(t)t.textContent=title||active;const s=document.getElementById('pageSub');if(s)s.textContent=subtitle||'Dados persistentes no Cloudflare D1';try{const p=await resolveProject();document.querySelectorAll('[data-project-name]').forEach(x=>x.textContent=p.project_name||'Implantação NUCCI ERP/TMS');return p}catch{const d=await loadReport();document.querySelectorAll('[data-project-name]').forEach(x=>x.textContent=d.project?.project_name||'Implantação NUCCI ERP/TMS');return d.project||{}}}
function openModal(id){document.getElementById(id)?.classList.add('on')}function closeModal(id){document.getElementById(id)?.classList.remove('on')}
async function softDelete(entity,id,onDone){if(!confirm('Enviar este registro para a lixeira?'))return;try{await api(`/api/madri-platform/${entity}/${encodeURIComponent(id)}`,{method:'DELETE'});showMessage('Registro enviado para a lixeira.');if(onDone)await onDone()}catch(e){showMessage(e.message,'error')}}
async function restore(entity,id,onDone){try{await api(`/api/madri-platform/${entity}/${encodeURIComponent(id)}/restore`,{method:'POST',body:'{}'});showMessage('Registro restaurado.');if(onDone)await onDone()}catch(e){showMessage(e.message,'error')}}
async function history(entity,id){try{const rows=await api(`/api/madri-platform/${entity}/${encodeURIComponent(id)}/history`),body=document.getElementById('historyBody');if(body)body.innerHTML=rows.length?rows.map(r=>`<div class="card" style="margin:7px 0"><b>${esc(r.action_type)}</b> · ${esc(r.created_at)} · ${esc(r.actor||'')}<pre style="white-space:pre-wrap;font-size:8px;max-height:220px;overflow:auto">${esc(r.snapshot_json)}</pre></div>`).join(''):'<div class="empty">Sem histórico.</div>';openModal('historyModal')}catch(e){showMessage(e.message,'error')}}
function operationalProgress(){if(!report)return 'N/D';const s=report.summary?.actions;if(s?.total)return `${Math.round(Number(s.done||0)*100/Number(s.total))}% das ações concluídas`;const total=Number(report.total||0),done=Number(report.counts?.['Concluído']||0);return total?`${Math.round(done*100/total)}% das ações concluídas`:'N/D'}
window.MADRIPlatform={api,esc,resolveProject,loadReport,init,badge,showMessage,openModal,closeModal,softDelete,restore,history,operationalProgress,officialRoutes,supportRoutes};
})();
