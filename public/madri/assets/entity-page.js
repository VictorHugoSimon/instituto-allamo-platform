(()=>{'use strict';const P=()=>window.MADRIPlatform,C=()=>window.MADRI_ENTITY;let rows=[],editing=null,trash=false,versionDoc=null;const get=id=>document.getElementById(id);function fieldHtml(f){const req=f.required?' required':'',val=f.default??'';if(f.type==='textarea')return `<div class="field ${f.full?'full':''}"><label>${P().esc(f.label)}</label><textarea id="ef_${f.name}"${req}>${P().esc(val)}</textarea></div>`;if(f.type==='select')return `<div class="field ${f.full?'full':''}"><label>${P().esc(f.label)}</label><select id="ef_${f.name}"${req}>${(f.options||[]).map(o=>`<option value="${P().esc(o)}">${P().esc(o)}</option>`).join('')}</select></div>`;if(f.type==='checkbox')return `<div class="field"><label>${P().esc(f.label)}</label><label style="font-size:10px;text-transform:none"><input id="ef_${f.name}" type="checkbox"> Sim</label></div>`;return `<div class="field ${f.full?'full':''}"><label>${P().esc(f.label)}</label><input id="ef_${f.name}" type="${f.type||'text'}" value="${P().esc(val)}"${req}></div>`}function value(f){const e=get('ef_'+f.name);return f.type==='checkbox'?e.checked:e.value}function set(f,v){const e=get('ef_'+f.name);if(!e)return;if(f.type==='checkbox')e.checked=!!Number(v);else e.value=v??f.default??''}function display(row,col){const v=row[col.key];if(col.badge)return P().badge(v);if(col.bool)return Number(v)?'Sim':'Não';return P().esc(String(v??'')||col.empty||'—')}function render(){const c=C(),q=(get('search')?.value||'').toLowerCase(),status=get('statusFilter')?.value||'',filtered=rows.filter(r=>(!q||JSON.stringify(r).toLowerCase().includes(q))&&(!status||String(r[c.statusKey||'status'])===status));get('rows').innerHTML=filtered.length?filtered.map(r=>{const id=P().esc(r.display_id||r.id),vers=c.entity==='documents'?` <button class="link" onclick="MADRIEntity.versions('${id}')">Versões</button>`:'';return `<tr>${c.columns.map(col=>`<td>${display(r,col)}</td>`).join('')}<td><button class="link" onclick="MADRIEntity.edit('${id}')">Editar</button> <button class="link" onclick="MADRIPlatform.history('${c.entity}','${id}')">Histórico</button>${vers} ${trash?`<button class="link" onclick="MADRIEntity.restore('${id}')">Restaurar</button>`:`<button class="link" onclick="MADRIEntity.remove('${id}')">Lixeira</button>`}</td></tr>`}).join(''):`<tr><td colspan="${c.columns.length+1}" class="empty">Nenhum registro.</td></tr>`;get('count').textContent=rows.length}async function load(){const c=C();await P().init(c.nav,c.title,c.subtitle);rows=await P().api(`/api/madri-platform/${c.entity}${trash?'?trash=1':''}`);render()}function open(row=null){editing=row;get('entityTitle').textContent=row?`Editar ${row.display_id}`:(C().singular||'Registro');C().fields.forEach(f=>set(f,row?row[f.name]:f.default));P().openModal('entityModal')}async function save(){const c=C(),body={};for(const f of c.fields)body[f.name]=value(f);try{if(editing)await P().api(`/api/madri-platform/${c.entity}/${encodeURIComponent(editing.display_id||editing.id)}`,{method:'PATCH',body:JSON.stringify(body)});else await P().api(`/api/madri-platform/${c.entity}`,{method:'POST',body:JSON.stringify(body)});P().closeModal('entityModal');P().showMessage(`${c.singular||'Registro'} salvo com sucesso.`);await load()}catch(e){P().showMessage(e.message,'error')}}function edit(id){open(rows.find(r=>String(r.display_id||r.id)===String(id))||null)}async function remove(id){await P().softDelete(C().entity,id,load)}async function restore(id){await P().restore(C().entity,id,load)}function toggleTrash(){trash=!trash;get('trashBtn').textContent=trash?'Ver ativos':'Lixeira';load().catch(e=>P().showMessage(e.message,'error'))}function ensureVersionModal(){if(get('versionModal'))return;const d=document.createElement('div');d.id='versionModal';d.className='modal';d.innerHTML=`<div class="modalbox"><h3 id="versionTitle">Versões</h3><div id="versionRows"></div><div class="section"><h3>Registrar nova versão</h3><div class="formgrid"><div class="field"><label>Versão</label><input id="vLabel" placeholder="v1.0"></div><div class="field"><label>Data</label><input id="vDate" type="date"></div><div class="field full"><label>Motivo</label><textarea id="vReason"></textarea></div><div class="field full"><label>Referência de conteúdo</label><input id="vContent"></div><div class="field full"><label>URL</label><input id="vUrl"></div><div class="field full"><label>Hash / referência</label><input id="vHash"></div></div></div><div class="modal-actions"><button class="btn" onclick="MADRIPlatform.closeModal('versionModal')">Fechar</button><button class="btn primary" onclick="MADRIEntity.addVersion()">Registrar versão</button></div></div>`;document.body.appendChild(d)}async function versions(id){if(C().entity!=='documents')return;versionDoc=rows.find(r=>String(r.display_id||r.id)===String(id));if(!versionDoc)return;ensureVersionModal();try{const list=await P().api(`/api/madri-platform/documents/${encodeURIComponent(id)}/versions`);get('versionTitle').textContent=`Versões · ${id}`;get('versionRows').innerHTML=list.length?list.map(v=>`<div class="card" style="margin:7px 0"><b>${P().esc(v.version_label)} ${Number(v.is_current)?'· VIGENTE':''}</b><p>${P().esc(v.version_date)} · ${P().esc(v.actor||'')} · ${P().esc(v.reason||'')}</p></div>`).join(''):'<div class="empty">Sem versões.</div>';get('vLabel').value=versionDoc.version_label||'';get('vDate').value=new Date().toISOString().slice(0,10);get('vContent').value=versionDoc.document_name||'';get('vUrl').value=versionDoc.url||'';get('vHash').value=versionDoc.hash_reference||'';P().openModal('versionModal')}catch(e){P().showMessage(e.message,'error')}}async function addVersion(){if(!versionDoc)return;const body={version_label:get('vLabel').value.trim(),version_date:get('vDate').value||undefined,reason:get('vReason').value.trim(),content_reference:get('vContent').value.trim(),url:get('vUrl').value.trim(),hash_reference:get('vHash').value.trim()};if(!body.version_label)return P().showMessage('Informe a versão.','error');try{await P().api(`/api/madri-platform/documents/${encodeURIComponent(versionDoc.display_id)}/versions`,{method:'POST',body:JSON.stringify(body)});P().showMessage('Nova versão registrada.');await load();await versions(versionDoc.display_id)}catch(e){P().showMessage(e.message,'error')}}async function init(){const c=C();document.title=c.title+' · MADRI';get('thead').innerHTML=`<tr>${c.columns.map(x=>`<th>${P().esc(x.label)}</th>`).join('')}<th>Ações</th></tr>`;get('entityFields').innerHTML=c.fields.map(fieldHtml).join('');if(c.statusOptions?.length)get('statusFilter').innerHTML='<option value="">Todos os status</option>'+c.statusOptions.map(x=>`<option>${P().esc(x)}</option>`).join('');else get('statusFilter').style.display='none';await load()}window.MADRIEntity={init,load,render,open,save,edit,remove,restore,toggleTrash,versions,addVersion};document.addEventListener('DOMContentLoaded',()=>init().catch(e=>P().showMessage(e.message,'error')));})();
